{
  "name": "youtube-practice-sections",
  "version": "1.0.0",
  "private": true,
  "description": "Static URL-only YouTube practice section looper for GitHub Pages.",
  "type": "module",
  "scripts": {
    "check": "find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test",
    "test-coverage": "node --test --experimental-test-coverage --test-coverage-include='src/**/*.js' --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=100"
  },
  "engines": {
    "node": ">=24"
  }
}
